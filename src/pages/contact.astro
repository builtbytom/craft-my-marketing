---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Contact" description="Ready to transform your business? Get in touch with our team for websites, automation, and custom technology solutions.">
  <!-- Hero Section -->
  <section class="relative min-h-[70vh] flex items-center overflow-hidden bg-white">
    <!-- Animated Background -->
    <div class="absolute inset-0">
      <div class="absolute inset-0 opacity-[0.06]" style="background-image: linear-gradient(#8B00FF 1px, transparent 1px), linear-gradient(90deg, #8B00FF 1px, transparent 1px); background-size: 50px 50px; animation: grid-move 20s linear infinite;"></div>
      <div class="absolute inset-0">
        <div class="absolute top-0 left-1/4 w-96 h-96 bg-purple/20 rounded-full filter blur-3xl animate-blob"></div>
        <div class="absolute top-1/3 right-1/4 w-96 h-96 bg-accent/15 rounded-full filter blur-3xl animate-blob animation-delay-2000"></div>
      </div>
    </div>

    <div class="relative z-10 w-full max-w-[1600px] mx-auto px-8 lg:px-12 xl:px-16 py-20">
      <div class="grid lg:grid-cols-12 gap-12">
        <div class="lg:col-span-8">
          <div class="inline-flex items-center gap-3 mb-10">
            <div class="w-2 h-2 bg-accent"></div>
            <span class="text-xs font-black text-black uppercase tracking-[0.2em]">GET IN TOUCH</span>
          </div>

          <h1 class="text-[2.5rem] md:text-[3.5rem] lg:text-[4.5rem] font-display font-black leading-[0.85] tracking-[-0.03em] mb-10">
            <span class="text-black">READY TO</span>
            <br/>
            <span class="text-purple">TRANSFORM</span>
            <br/>
            <span class="text-purple">YOUR BUSINESS?</span>
          </h1>

          <p class="text-lg md:text-xl text-black/60 mb-16 max-w-2xl leading-relaxed font-bold">
            Let's discuss how we can help you grow with cutting-edge websites and smart automation solutions.
          </p>

          <div class="flex flex-wrap gap-6">
            <div class="flex items-center gap-3">
              <div class="w-3 h-3 bg-accent rounded-full"></div>
              <span class="font-black text-black uppercase tracking-wider text-sm">Free Strategy Call</span>
            </div>
            <div class="flex items-center gap-3">
              <div class="w-3 h-3 bg-pink rounded-full"></div>
              <span class="font-black text-black uppercase tracking-wider text-sm">Custom Solutions</span>
            </div>
            <div class="flex items-center gap-3">
              <div class="w-3 h-3 bg-purple rounded-full"></div>
              <span class="font-black text-black uppercase tracking-wider text-sm">Connecticut Focused</span>
            </div>
          </div>
        </div>

        <!-- Contact Stats -->
        <div class="lg:col-span-4 relative">
          <div class="bg-purple text-white p-8 rounded-2xl shadow-2xl">
            <div class="text-center mb-6">
              <div class="text-2xl font-black mb-2">RESPONSE TIMES</div>
            </div>
            <div class="space-y-4">
              <div class="flex justify-between items-center">
                <span class="text-sm font-bold">General Inquiries</span>
                <span class="text-xl font-black text-accent">Same Day</span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm font-bold">Project Proposals</span>
                <span class="text-xl font-black text-accent">24-48 Hours</span>
              </div>
              <div class="flex justify-between items-center">
                <span class="text-sm font-bold">Client Support</span>
                <span class="text-xl font-black text-accent">Within 4 Hours</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Form -->
  <section class="py-20 bg-gray-50">
    <div class="max-w-[1600px] mx-auto px-8 lg:px-12 xl:px-16">
      
      <div class="grid lg:grid-cols-2 gap-16 items-start">
        <!-- Form -->
        <div class="bg-white p-8 md:p-12 border-4 border-purple shadow-2xl">
          <h2 class="text-3xl md:text-4xl font-black mb-6 text-purple">Let's Start the Conversation</h2>
          <p class="text-black/70 mb-8 text-lg">Tell us about your challenge and we'll figure out the best solution together. No sales pitch - just honest advice.</p>
          
          <form class="space-y-6" netlify data-netlify="true" name="contact-inquiry">
            <div class="grid md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-black uppercase tracking-wider mb-2 text-black">Your Name *</label>
                <input type="text" name="name" required class="w-full p-4 border-2 border-gray-200 focus:border-purple outline-none transition-colors">
              </div>
              <div>
                <label class="block text-sm font-black uppercase tracking-wider mb-2 text-black">Company Name</label>
                <input type="text" name="company" class="w-full p-4 border-2 border-gray-200 focus:border-purple outline-none transition-colors">
              </div>
            </div>
            
            <div class="grid md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-black uppercase tracking-wider mb-2 text-black">Email *</label>
                <input type="email" name="email" required class="w-full p-4 border-2 border-gray-200 focus:border-purple outline-none transition-colors">
              </div>
              <div>
                <label class="block text-sm font-black uppercase tracking-wider mb-2 text-black">Phone</label>
                <input type="tel" name="phone" class="w-full p-4 border-2 border-gray-200 focus:border-purple outline-none transition-colors">
              </div>
            </div>

            <div>
              <label class="block text-sm font-black uppercase tracking-wider mb-2 text-black">What's your biggest challenge right now? *</label>
              <textarea name="challenge" required rows="6" 
                placeholder="Tell us about your business, what's frustrating you, what you're trying to achieve, or any questions you have. We're here to help figure out the best path forward."
                class="w-full p-4 border-2 border-gray-200 focus:border-purple outline-none transition-colors resize-none"></textarea>
            </div>

            <div class="grid md:grid-cols-2 gap-6">
              <div>
                <label class="block text-sm font-black uppercase tracking-wider mb-2 text-black">Timeline</label>
                <select name="timeline" class="w-full p-4 border-2 border-gray-200 focus:border-purple outline-none transition-colors">
                  <option value="">Select Timeline</option>
                  <option value="asap">ASAP - I need help now</option>
                  <option value="month">Within 1 Month</option>
                  <option value="quarter">Within 3 Months</option>
                  <option value="exploring">Just Exploring Options</option>
                </select>
              </div>
              <div>
                <label class="block text-sm font-black uppercase tracking-wider mb-2 text-black">Rough Budget Range</label>
                <select name="budget" class="w-full p-4 border-2 border-gray-200 focus:border-purple outline-none transition-colors">
                  <option value="">Select Range (Optional)</option>
                  <option value="under-5k">Under $5,000</option>
                  <option value="5k-15k">$5,000 - $15,000</option>
                  <option value="15k-30k">$15,000 - $30,000</option>
                  <option value="30k+">$30,000+</option>
                  <option value="discuss">Let's Discuss</option>
                </select>
              </div>
            </div>

            <button type="submit" class="w-full bg-purple text-white py-6 px-8 font-black text-lg uppercase tracking-wider hover:bg-purple-dark hover:scale-105 transition-all duration-300 shadow-lg">
              Get My Free Strategy Call →
            </button>
          </form>
        </div>

        <!-- Info Side -->
        <div class="space-y-8">
          <div>
            <h3 class="text-2xl font-black mb-4 text-black">What Happens Next?</h3>
            <div class="space-y-4">
              <div class="flex items-start gap-4">
                <div class="w-8 h-8 bg-purple text-white rounded-full flex items-center justify-center font-black text-sm flex-shrink-0 mt-1">1</div>
                <div>
                  <h4 class="font-black text-lg mb-1">We Review Your Challenge</h4>
                  <p class="text-black/70">We'll look at what you've shared and do some quick research on your industry and competitors.</p>
                </div>
              </div>
              <div class="flex items-start gap-4">
                <div class="w-8 h-8 bg-accent text-black rounded-full flex items-center justify-center font-black text-sm flex-shrink-0 mt-1">2</div>
                <div>
                  <h4 class="font-black text-lg mb-1">15-Minute Discovery Call</h4>
                  <p class="text-black/70">Quick call to understand your situation and see if we're a good fit. No sales pitch - just honest advice.</p>
                </div>
              </div>
              <div class="flex items-start gap-4">
                <div class="w-8 h-8 bg-pink text-white rounded-full flex items-center justify-center font-black text-sm flex-shrink-0 mt-1">3</div>
                <div>
                  <h4 class="font-black text-lg mb-1">Custom Strategy</h4>
                  <p class="text-black/70">If we can help, we'll create a specific plan for your situation. If not, we'll point you in the right direction.</p>
                </div>
              </div>
            </div>
          </div>

          <div class="bg-black text-white p-8 rounded-2xl">
            <h3 class="text-xl font-black mb-4 text-accent">Fair Warning:</h3>
            <p class="text-white/90 leading-relaxed">
              We're not the cheapest option, and we don't take on every project. We only work with businesses that are serious about results and ready to invest in real solutions.
            </p>
            <p class="text-white/90 leading-relaxed mt-4">
              <strong class="text-accent">But if we're a fit</strong> - you'll get systems that actually work and support that doesn't disappear after launch.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Quick Contact Options -->
  <section class="py-16 bg-white">
    <div class="max-w-[1600px] mx-auto px-8 lg:px-12 xl:px-16">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-black mb-4">Prefer to Talk First?</h2>
        <p class="text-xl text-black/70">Sometimes a quick conversation is worth a thousand forms.</p>
      </div>

      <div class="grid md:grid-cols-3 gap-8">
        <!-- Phone -->
        <div class="bg-white p-8 text-center border-2 border-gray-200 hover:border-purple transition-colors group">
          <div class="w-16 h-16 bg-purple text-white rounded-full flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-black mb-2">Call Us</h3>
          <p class="text-black/70 mb-4">Quick questions? Let's chat.</p>
          <a href="tel:475.400.1407" class="text-purple font-bold hover:underline">475.400.1407</a>
        </div>

        <!-- Email -->
        <div class="bg-white p-8 text-center border-2 border-gray-200 hover:border-teal transition-colors group">
          <div class="w-16 h-16 bg-teal text-black rounded-full flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 4.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-black mb-2">Email Us</h3>
          <p class="text-black/70 mb-4">Send us your thoughts.</p>
          <a href="mailto:hello@cmmct.com" class="text-teal font-bold hover:underline">hello@cmmct.com</a>
        </div>

        <!-- Calendar -->
        <div class="bg-white p-8 text-center border-2 border-gray-200 hover:border-pink transition-colors group">
          <div class="w-16 h-16 bg-pink text-white rounded-full flex items-center justify-center mx-auto mb-4 group-hover:scale-110 transition-transform">
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-black mb-2">Book a Call</h3>
          <p class="text-black/70 mb-4">Ready for a strategy session?</p>
          <a href="https://calendly.com/cmmct/consultation" class="text-pink font-bold hover:underline" target="_blank" rel="noopener">Schedule Now</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Location & Hours -->
  <section class="py-16 bg-gray-50">
    <div class="max-w-[1600px] mx-auto px-8 lg:px-12 xl:px-16">
      <div class="grid md:grid-cols-2 gap-12 items-center">
        <div>
          <h2 class="text-3xl md:text-4xl font-black mb-6">Based in Connecticut,<br>Serving Everywhere</h2>
          <p class="text-xl text-black/70 mb-8">
            We love working with Connecticut businesses, but our digital solutions work anywhere. 
            Remote collaboration is our specialty.
          </p>
          
          <div class="space-y-4">
            <div class="flex items-start gap-4">
              <div class="w-6 h-6 bg-purple rounded-full flex-shrink-0 mt-1"></div>
              <div>
                <h3 class="font-bold text-lg">Connecticut Focused</h3>
                <p class="text-black/70">We understand the local market and business landscape</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <div class="w-6 h-6 bg-teal rounded-full flex-shrink-0 mt-1"></div>
              <div>
                <h3 class="font-bold text-lg">Nationally Available</h3>
                <p class="text-black/70">Digital solutions work everywhere - location is no barrier</p>
              </div>
            </div>
            <div class="flex items-start gap-4">
              <div class="w-6 h-6 bg-pink rounded-full flex-shrink-0 mt-1"></div>
              <div>
                <h3 class="font-bold text-lg">Remote First</h3>
                <p class="text-black/70">Efficient collaboration tools keep projects moving fast</p>
              </div>
            </div>
          </div>
        </div>

        <div class="bg-white p-8 border-2 border-gray-200 rounded-2xl shadow-lg">
          <h3 class="text-2xl font-black mb-6">Our Promise</h3>
          <div class="space-y-4">
            <div class="flex items-start gap-3">
              <div class="w-2 h-2 bg-purple rounded-full mt-3 flex-shrink-0"></div>
              <p class="text-black/80"><strong>Plain English:</strong> If we can't explain it simply, it's too complicated.</p>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-2 h-2 bg-accent rounded-full mt-3 flex-shrink-0"></div>
              <p class="text-black/80"><strong>Real Support:</strong> We answer our phones and respond to emails.</p>
            </div>
            <div class="flex items-start gap-3">
              <div class="w-2 h-2 bg-pink rounded-full mt-3 flex-shrink-0"></div>
              <p class="text-black/80"><strong>Systems That Work:</strong> We build solutions that function under pressure.</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Form focus states */
  input:focus, select:focus, textarea:focus {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.1);
  }

  /* Grid animation */
  @keyframes grid-move {
    0% { transform: translate(0, 0); }
    100% { transform: translate(50px, 50px); }
  }

  /* Blob animation */
  @keyframes blob {
    0%, 100% { transform: translate(0px, 0px) scale(1); }
    33% { transform: translate(30px, -50px) scale(1.1); }
    66% { transform: translate(-20px, 20px) scale(0.9); }
  }

  .animate-blob {
    animation: blob 20s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }
</style>

<script>
  // Enhanced form interactions
  document.addEventListener('DOMContentLoaded', function() {
    // Add loading states to form submissions
    const form = document.querySelector('form');
    if (form) {
      form.addEventListener('submit', function(e) {
        const button = form.querySelector('button[type="submit"]');
        const originalText = button.textContent;
        button.textContent = 'Sending...';
        button.disabled = true;
        
        // Re-enable after a delay (Netlify will handle the actual submission)
        setTimeout(() => {
          button.textContent = originalText;
          button.disabled = false;
        }, 3000);
      });
    }
  });
</script>
</Layout>